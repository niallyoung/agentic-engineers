=============================================================================
AGENTIC-ENGINEERS CODEBASE ANALYSIS - EXECUTIVE SUMMARY
=============================================================================

ANALYSIS COMPLETED: 2026-05-30
FOCUS: CI Failures, Environment Differences, Path Validation, Symlink Handling

=============================================================================
CRITICAL FINDINGS (BLOCKS CI)
=============================================================================

1. DUAL IMPLEMENTATION WITH CONFLICTING CONTRACTS
   ✗ File 1: renderer/validate_skills.py (lines 39-148)
     - Returns Dict on all inputs
     - Never raises exceptions
   
   ✗ File 2: src/skills/_meta/queue-path-validator/queue_path_validator.py (lines 40-222)
     - Raises AssertionError on contract violations
     - Different error message format
   
   IMPACT: 16 of 33 tests fail in test_queue_path_validator.py
   TEST FAILURES:
   - test_valid_canonical_path_absolute → AssertionError (expected dict)
   - test_valid_canonical_path_relative → Different error message
   - test_reject_empty_path → Raises AssertionError (expected dict)
   - test_reject_none_path → Raises AssertionError (expected dict)
   + 12 more path validation and subdir tests fail

2. CONTRACT VIOLATION: Type Mismatch
   DECLARED: Returns Dict[str, Any] with keys (valid, session_id, harness, subdir, error)
   ACTUAL:   Raises AssertionError on None/empty string
   CODE:     Lines 77-82 in File 2 raise AssertionError instead of returning error dict

3. SYMLINK CONFIGURATION TIMING (CI ENVIRONMENT)
   CURRENT: git config --global core.symlinks true (line 22 in ci.yml)
   PROBLEM: This runs AFTER checkout (line 15)
   IMPACT: Git checkout may skip symlink creation before config is applied
   FIX: Move git config before actions/checkout@v4

=============================================================================
ENVIRONMENT DIFFERENCES
=============================================================================

macOS (Local):
  - OS: Darwin (APFS file system)
  - Python: 3.7.4
  - Symlinks: Native support, no special permissions
  - Path resolution: APFS native symlink handling
  - Status: ✓ Works correctly

Linux (GitHub Actions - CI):
  - OS: Ubuntu Latest (ext4/overlayfs in container)
  - Python: 3.11
  - Symlinks: Requires CAP_SYS_ADMIN (available in GitHub Actions runners)
  - Path resolution: ext4 native symlink handling
  - Status: ✓ Works correctly IF properly configured

DETECTION METHOD (Both):
  - os.path.islink() - POSIX standard, works on both
  - Path.is_symlink() - Pathlib wrapper, works on both
  - Path.resolve() - Handles symlink loops on both

CRITICAL ISSUE:
  Both implementations check: if os.path.exists() AND os.path.islink()
  PROBLEM: Broken symlinks return False for exists(), skip detection
  FIX: Check islink() independently of exists()

=============================================================================
PATH VALIDATION ANALYSIS
=============================================================================

Regex Pattern (Both files):
  r'^~?/?\.agentic-engineers/artifacts/([a-z0-9\-]+)/([a-z0-9\-]+)/queue/?$'
  
  Parts:
  - ~?/?                                      Optional tilde + slash
  - \.agentic-engineers/artifacts/            Literal path
  - ([a-z0-9\-]+)/                            Capture: session_id
  - ([a-z0-9\-]+)/                            Capture: harness
  - queue/?$                                  Literal queue dir

Test Coverage:
  ✓ Valid canonical paths (macOS + CI)
  ✓ Legacy path rejection
  ✓ Path traversal prevention (../, //)
  ✗ Contract validation (None, empty string) - FAILS in File 2
  ✗ Error message format consistency - FAILS
  ✗ Subdirectory validation - Some FAIL

=============================================================================
SYMLINK HANDLING
=============================================================================

Implementation Status:
  macOS: ✓ All symlink operations work
  CI: ✓ All symlink operations work (with config)

Test Coverage:
  ✓ Rejects symlinks to /etc/passwd (external)
  ✓ Accepts symlinks within repo boundary
  ✓ Detects chained symlinks (s1 → s2 → external)
  ✓ Handles broken symlinks gracefully
  ✓ Detects symlink loops (a → b → a)
  ✗ Doesn't test broken symlink detection due to AND operator

Gaps:
  - No test for broken symlinks (target doesn't exist)
  - No test verifying symlink creation works in CI
  - No platform-specific tests

=============================================================================
TESTING INFRASTRUCTURE
=============================================================================

Root Config (/conftest.py):
  - Adds src/ to sys.path ✓
  - Adds src/skills/ to sys.path ✓
  - Sets PYTHONPATH for subprocesses ✓

Tests Config (/tests/conftest.py):
  - Clears .pytest_cache ✓
  - Audits test source integrity ✓
  - Provides DELEGATE/HANDBACK factories ✓
  - Provides queue fixtures ✓

Gaps:
  - ✗ Clears .pytest_cache only, NOT __pycache__
  - ✗ No platform detection tests
  - ✗ No CI-specific test isolation
  - ✗ No contract validation tests
  - ✗ No symlink lifecycle verification

=============================================================================
CONTRACT VIOLATIONS SUMMARY
=============================================================================

Violation 1: Dual Implementation
  LOCATION: renderer/validate_skills.py vs src/skills/_meta/queue-path-validator/queue_path_validator.py
  ISSUE: Same function, different contracts
  RESOLUTION: Unify implementations

Violation 2: Return Type Mismatch
  DECLARED: Always returns Dict[str, Any]
  ACTUAL: Raises AssertionError on contract errors
  FIX: Change all exception paths to return error dicts

Violation 3: Symlink Detection Gap
  CURRENT: if os.path.exists() AND os.path.islink()
  ISSUE: Misses broken symlinks (exists=False, islink=True)
  FIX: Check islink() independently

=============================================================================
CI PIPELINE FLOW & FAILURES
=============================================================================

Steps:
  1. Checkout code ← symlinks NOT yet configured
  2. git config core.symlinks true ← TOO LATE
  3. Setup Python 3.11
  4. Install dependencies
  5. make lint ✓
  6. make render-* ✓
  7. make test ✗ FAILS HERE (16 test failures)
  8. make verify (unreached)

Failure Point:
  - pytest imports test_queue_path_validator.py
  - Tests call validate_queue_path(None)
  - File 2 raises AssertionError
  - Tests expect dict with 'valid': False
  - RESULT: AssertionError: "Path must be a non-empty string"

=============================================================================
PRIORITY 1 FIXES (CRITICAL - BLOCKS CI)
=============================================================================

Fix 1: Unify queue_path_validator implementations
  - Keep File 2 as canonical (more flexible type handling)
  - Remove File 1 from renderer/validate_skills.py
  - Update File 2 to match contract: ALWAYS return Dict, NEVER raise

  Code change needed:
  ```python
  def validate_queue_path(path: Union[Path, str]) -> Dict[str, Any]:
      # Instead of raising:
      if not isinstance(path, (Path, str)):
          return {'valid': False, 'error': 'Must be Path or str', ...}
      
      if not path or (isinstance(path, str) and not path.strip()):
          return {'valid': False, 'error': 'Path must be non-empty', ...}
  ```

Fix 2: Move symlink config before checkout in CI
  Current (.github/workflows/ci.yml line 15-22):
    - uses: actions/checkout@v4
    - run: git config --global core.symlinks true
  
  New:
    - run: git config --global core.symlinks true
    - uses: actions/checkout@v4

Fix 3: Fix symlink detection for broken symlinks
  Current: if os.path.exists(normalized) and os.path.islink(normalized)
  Change: if os.path.islink(normalized)

=============================================================================
PRIORITY 2 FIXES (HIGH - IMPROVES TESTING)
=============================================================================

Fix 4: Clear __pycache__ in pytest_configure
  Add to tests/conftest.py pytest_configure():
    shutil.rmtree('__pycache__', ignore_errors=True)

Fix 5: Add platform detection tests
  @pytest.mark.skipif(platform.system() == 'Windows', reason='Unix-only')
  def test_symlink_on_unix(temp_repo):
      # Verify symlinks work on this platform

Fix 6: Add CI-specific test validation
  @pytest.mark.ci_only
  def test_git_symlinks_enabled():
      result = os.system('git config core.symlinks')
      assert result == 'true'

=============================================================================
FILE LOCATIONS & ISSUES
=============================================================================

src/skills/_meta/queue-path-validator/queue_path_validator.py
  - Status: Canonical validator ← File 2
  - Issue: Raises on contract violation ✗
  - Lines: 40-222
  
renderer/validate_skills.py
  - Status: Duplicate implementation ← File 1
  - Issue: Should be removed/unified ✗
  - Lines: 39-148

tests/test_queue_path_validator.py
  - Status: Test suite
  - Issue: Expects File 1 contract, runs File 2 ✗
  - Failures: 16 of 33 tests

.github/workflows/ci.yml
  - Status: CI pipeline
  - Issue: Symlink config too late ✗
  - Line: 22 (should be before 15)

conftest.py
  - Status: Root pytest config
  - Issue: Path setup OK ✓

tests/conftest.py
  - Status: Test pytest config
  - Issue: Cache clearing incomplete ✗
  - Line: 48-54 (missing __pycache__ cleanup)

=============================================================================
TEST RESULTS SUMMARY
=============================================================================

Queue Path Validator Tests: 16 of 33 FAIL
  - 5 fail on contract violations (None, empty string)
  - 6 fail on error message format mismatch
  - 5 fail on pattern matching differences

Validate Skills Tests: MOSTLY PASS
  - ✓ Rejects external symlinks
  - ✓ Accepts internal symlinks
  - ✓ Detects chained symlinks
  - ✓ Handles broken symlinks
  - ✓ Detects symlink loops

Symlink Detection: Works on both macOS and Linux
  - ✓ os.path.islink() available on both
  - ✓ Path.is_symlink() available on both
  - ✓ Path.resolve() handles loops on both
  - ✗ But tests don't verify CI configuration

=============================================================================
ESTIMATED IMPACT & EFFORT
=============================================================================

Fix 1 (Unify implementations):      Effort: 2-3 hours    Impact: CRITICAL
Fix 2 (Move symlink config):        Effort: 15 min       Impact: CRITICAL
Fix 3 (Fix symlink detection):      Effort: 30 min       Impact: HIGH
Fix 4 (Clear __pycache__):          Effort: 10 min       Impact: MEDIUM
Fix 5 (Platform detection tests):   Effort: 1-2 hours    Impact: MEDIUM
Fix 6 (CI-specific tests):          Effort: 1 hour       Impact: MEDIUM

Total Critical Path: ~3.5 hours (Fixes 1-3) → CI unblocked

=============================================================================
RECOMMENDATION
=============================================================================

IMMEDIATE ACTION (Next commit):
  1. Apply Fix 2 (move symlink config) - 15 minutes
  2. Apply Fix 1 (unify implementations) - 2-3 hours
  3. Apply Fix 3 (fix symlink detection) - 30 minutes
  4. Run tests to verify: make test

FOLLOW-UP (Next sprint):
  5. Apply Fix 4 (cache cleanup)
  6. Apply Fix 5 (platform tests)
  7. Apply Fix 6 (CI tests)
  8. Add documentation: .github/workflows/ENVIRONMENT.md

STATUS: This analysis is COMPLETE and READY for implementation.

=============================================================================
