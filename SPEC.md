---
name: Agentic Engineers Implementation Specification
description: Pointer to the canonical specification at docs/SPEC.md
version: 2.0
updated: 2026-08-13
phase: 5.10 (Monitoring & Continuous Improvement)
status: Pointer — canonical spec is docs/SPEC.md
type: specification
---

# Agentic Engineers Implementation Specification

> ➡️ **The canonical specification lives at [`docs/SPEC.md`](docs/SPEC.md).**

This root-level file is **not** the specification. The authoritative `docs/SPEC.md`
is the file that:

- is protected by the `spec-management` skill (all changes go through a
  proposal → analysis → approval → changelog → audit workflow),
- is validated by `spec-validator` and the git hooks (pre-commit, commit-msg,
  pre-push), and
- is deployed by the render pipeline to `dist/specs/SPEC.md`.

**Do not edit this file.** Propose changes against `docs/SPEC.md` via the
`spec-management` skill (Principal / Security / Lead Engineer only).

## History

This file previously held a forked, drifting copy of the specification. As of
**2026-06-12** its remaining unique content — the model-governance sections
(positive-enforcement philosophy, the `.githooks/LOCKED_MODELS.sh` single source
of truth, and the Model Switch Process) — was migrated into `docs/SPEC.md` under
the *Model Naming & Harness Compatibility (LOCKED SPEC)* section via
spec-management proposal **SPEC-2026-001**, and this file was reduced to the
pointer you are reading. See the Update Log in `docs/SPEC.md` for details.
