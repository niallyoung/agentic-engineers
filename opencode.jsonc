// agentic-engineers OpenCode configuration
// Integrates git hooks with OpenCode commands for SDLC enforcement
{
  "$schema": "https://opencode.ai/config.json",
  "instructions": ["AGENTS.md"],
  "default_agent": "orchestrator",
  "model": "anthropic/claude-haiku-4-5",

  // Compaction settings for token efficiency
  "compaction": {
    "auto": true,
    "reserved": 30000
  },

  // Idle-loop queue cooperation (Phase G-1): when the harness is idle for
  // interval_seconds, invoke the orchestrator-scheduler SKILL to poll the
  // session queue once. File-based locking coordinates with other harnesses.
  "idle_loop": {
    "enabled": true,
    "interval_seconds": 180,
    "action": "invoke_skill",
    "skill": "orchestrator-scheduler",
    "args": ["--poll-once"],
    "backoff_intervals": [5, 30, 180, 600],
    "watch_enabled": true,
    "watch_poll_seconds": 0.5
  },

  // Permission model for tool access
  "permission": {
    "read": "allow",
    "edit": "allow",
    "bash": "allow",
    "task": "allow",
    "glob": "allow",
    "grep": "allow",
    "webfetch": "allow"
  },

  // OpenCode commands for SDLC workflow
  "command": {
    "sdlc-check": {
      "description": "Validate SDLC workflow compliance — check queue, DELEGATEs, and HANDBACKs",
      "agent": "orchestrator",
      "subtask": true,
      "template": "Validate SDLC workflow compliance by checking the queue status, reviewing all pending DELEGATEs and HANDBACKs, verifying protocol compliance, and ensuring no protocol violations."
    },
    "hooks-install": {
      "description": "Install git enforcement hooks (core.hooksPath = .githooks)",
      "agent": "orchestrator",
      "template": "Install and configure git enforcement hooks from .githooks/ directory. Set core.hooksPath = .githooks and verify all hooks are executable and properly configured."
    },
    "queue-status": {
      "description": "Review pending DELEGATEs and HANDBACKs in the queue",
      "agent": "orchestrator",
      "subtask": true,
      "template": "Review the current state of the task queue. List all pending DELEGATEs and HANDBACKs, their status, age, and any issues that need attention."
    }
  }

}
