---
handoff_type: DELEGATE
task_id: 2026-05-05-week2-engineer-4-config-enforcement
timestamp: 2026-04-28T19:00:00Z
from_agent: Orchestrator (Haiku)
to_agent: Engineer (Sonnet)
role: Engineer
model: claude-sonnet-4-6
effort: high

scope: >
  Implement Config Enforcement skill — auto-fixing configuration issues agent.
  Create agentic-engineers/skills/config-enforcement.md with complete DELEGATE/HANDBACK
  protocol. Agent applies fixes to config deviations, validates changes, escalates
  uncertain fixes for human review.

context:
  - Design spec: agentic-engineers/orchestration/AGENT-SPECS-WEEK1-DESIGNS.md (Agent 4)
  - Role: Senior Engineer (applies fixes, handles edge cases)
  - Input source: Config Audit Agent (provides deviations list)
  - Handoff protocol: HANDOFF.md
  - Integration: Re-runs Config Audit after applying fixes
  - Self-healing philosophy: auto-fix high-confidence, escalate low-confidence
  - Architecture Remediation Week 2: 2026-05-05 to 2026-05-12
  - Budget: ~2500 tokens estimated

plan:
  1. Review AGENT-SPECS-WEEK1-DESIGNS.md Agent 4 specification
  2. Review Config Audit output format (deviations with confidence scores)
  3. Create skills/config-enforcement.md with structure:
     - Agent purpose + scope
     - DELEGATE block specification (deviations[], dry_run, auto_approve_threshold)
     - HANDBACK block specification (fixes_applied, fixes_escalated, validation_results)
     - Fix application logic (file modifications based on remediation guidance)
     - Confidence threshold logic (only apply if >= threshold)
     - Validation after each fix (make lint, make test)
     - Escalation reasoning (why certain fixes require human review)
     - Git diff generation (show all changes clearly)
     - Re-audit integration (call Config Audit after fixes)
  4. Support dry-run mode (validate without applying)
  5. Ensure all applied fixes pass validation
  6. Generate clear git diffs for human review
  7. Test with sample deviations
  8. Return HANDBACK with deliverable + validation

success_criteria:
  - Skill document created (400-500 lines)
  - DELEGATE spec matches design spec
  - HANDBACK spec includes all fields (fixes_applied, fixes_escalated, git_diff)
  - High-confidence fixes applied automatically
  - Low-confidence fixes escalated with clear reasoning
  - All applied fixes pass validation (lint/test)
  - Git diff is clear and reviewable
  - Config Audit re-run shows improved compliance score
  - Dry-run mode works correctly
  - Ready for self-healing phase in Quality Gate Orchestrator

success_metrics:
  - make lint passes
  - make test passes
  - Skill document is clear and actionable
  - Design spec -> implementation traceable
  - Escalations have defensible reasoning

notes: >
  This agent implements the "self-healing" philosophy of the remediation. Balance
  between automation (reduce human burden) and safety (escalate when uncertain).
  Prioritize correctness over aggression in auto-fixing.

budget_context:
  session_pct_at_delegation: 48.0
  estimated_tokens_needed: 2500
  hours_until_reset: 5.5
  status: GREEN
  recommendation: "Sonnet appropriate for file modification + validation"

---
