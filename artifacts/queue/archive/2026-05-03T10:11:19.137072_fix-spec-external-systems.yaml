---
handoff_type: DELEGATE
task_id: fix-spec-external-systems-2026-05-03
role: Engineer
model: claude-sonnet-4-6
effort: medium
scope: |
  Fix undefined "external systems" language in docs/SPEC.md that allows CI/CD to bypass queue.
  Currently: "external systems" can write DELEGATE files directly to artifacts/queue/incoming/
  This should be: only humans and Orchestrator can write to queue.

context:
  - File: $REPO_ROOT/docs/SPEC.md
  - Lines: 218 (Glossary), 113 (Prohibited Activities), and related sections
  - Issue: "external systems" is undefined; CI/CD could write DELEGATE directly and bypass SPEC constraints

plan:
  - 1. Open docs/SPEC.md and find line 218 (Glossary, DELEGATE Block entry)
  - 2. Current text: "Placed in `artifacts/queue/incoming/` by external systems."
  - 3. Replace with: "Placed in `artifacts/queue/incoming/` only by humans or the Orchestrator itself. Automated external systems MUST NOT write directly to the queue."
  - 4. Find line 113 (What NOT to Do section, first bullet)
  - 5. Current text: "Do NOT allow CI/CD or external systems to invoke scripts directly"
  - 6. Add NEW bullet after line 113: "❌ Do NOT allow any automated external system to write files directly to `artifacts/queue/incoming/` — all queue entries originate from humans or the Orchestrator"
  - 7. Run: git diff docs/SPEC.md (confirm both changes)
  - 8. Commit with message: "Fix SPEC.md lines 218 & 113: Define external systems; prohibit direct queue writes"

success_criteria:
  - Line 218 Glossary now reads: "only by humans or the Orchestrator"
  - Line 113+ includes new prohibition on external system queue writes
  - Both changes make clear: ONLY humans and Orchestrator can write to queue
  - Change committed

---
